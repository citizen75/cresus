# Local development overrides
# Copy to .env.local to override .env settings

# Use non-standard ports to avoid conflicts
API_PORT=9000
FRONT_PORT=3000

# Allow external connections
API_HOST=0.0.0.0
FRONT_HOST=0.0.0.0

# Enable cron for testing
GATEWAY_CRON_ENABLED=true
